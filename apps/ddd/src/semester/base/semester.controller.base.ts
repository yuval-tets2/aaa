/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SemesterService } from "../semester.service";
import { SemesterCreateInput } from "./SemesterCreateInput";
import { SemesterWhereInput } from "./SemesterWhereInput";
import { SemesterWhereUniqueInput } from "./SemesterWhereUniqueInput";
import { SemesterFindManyArgs } from "./SemesterFindManyArgs";
import { SemesterUpdateInput } from "./SemesterUpdateInput";
import { Semester } from "./Semester";
import { CourseFindManyArgs } from "../../course/base/CourseFindManyArgs";
import { Course } from "../../course/base/Course";
import { CourseWhereUniqueInput } from "../../course/base/CourseWhereUniqueInput";

export class SemesterControllerBase {
  constructor(protected readonly service: SemesterService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Semester })
  async create(@common.Body() data: SemesterCreateInput): Promise<Semester> {
    return await this.service.create({
      data: {
        ...data,

        plan: {
          connect: data.plan,
        },
      },
      select: {
        color: true,
        id: true,
        locked: true,

        plan: {
          select: {
            id: true,
          },
        },

        semester: true,
        year: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Semester] })
  @ApiNestedQuery(SemesterFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Semester[]> {
    const args = plainToClass(SemesterFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        color: true,
        id: true,
        locked: true,

        plan: {
          select: {
            id: true,
          },
        },

        semester: true,
        year: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Semester })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: SemesterWhereUniqueInput
  ): Promise<Semester | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        color: true,
        id: true,
        locked: true,

        plan: {
          select: {
            id: true,
          },
        },

        semester: true,
        year: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Semester })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: SemesterWhereUniqueInput,
    @common.Body() data: SemesterUpdateInput
  ): Promise<Semester | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          plan: {
            connect: data.plan,
          },
        },
        select: {
          color: true,
          id: true,
          locked: true,

          plan: {
            select: {
              id: true,
            },
          },

          semester: true,
          year: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Semester })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: SemesterWhereUniqueInput
  ): Promise<Semester | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          color: true,
          id: true,
          locked: true,

          plan: {
            select: {
              id: true,
            },
          },

          semester: true,
          year: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/courses")
  @ApiNestedQuery(CourseFindManyArgs)
  async findManyCourses(
    @common.Req() request: Request,
    @common.Param() params: SemesterWhereUniqueInput
  ): Promise<Course[]> {
    const query = plainToClass(CourseFindManyArgs, request.query);
    const results = await this.service.findCourses(params.id, {
      ...query,
      select: {
        code: true,
        color: true,
        id: true,
        locked: true,
        prereqOverriden: true,

        semester: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/courses")
  async connectCourses(
    @common.Param() params: SemesterWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/courses")
  async updateCourses(
    @common.Param() params: SemesterWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/courses")
  async disconnectCourses(
    @common.Param() params: SemesterWhereUniqueInput,
    @common.Body() body: CourseWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      courses: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
