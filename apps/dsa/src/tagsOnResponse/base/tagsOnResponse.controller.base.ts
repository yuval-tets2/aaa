/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { TagsOnResponseService } from "../tagsOnResponse.service";
import { TagsOnResponseCreateInput } from "./TagsOnResponseCreateInput";
import { TagsOnResponseWhereInput } from "./TagsOnResponseWhereInput";
import { TagsOnResponseWhereUniqueInput } from "./TagsOnResponseWhereUniqueInput";
import { TagsOnResponseFindManyArgs } from "./TagsOnResponseFindManyArgs";
import { TagsOnResponseUpdateInput } from "./TagsOnResponseUpdateInput";
import { TagsOnResponse } from "./TagsOnResponse";

export class TagsOnResponseControllerBase {
  constructor(protected readonly service: TagsOnResponseService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TagsOnResponse })
  async create(
    @common.Body() data: TagsOnResponseCreateInput
  ): Promise<TagsOnResponse> {
    return await this.service.create({
      data: {
        ...data,

        response: {
          connect: data.response,
        },

        tag: {
          connect: data.tag,
        },
      },
      select: {
        id: true,

        response: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [TagsOnResponse] })
  @ApiNestedQuery(TagsOnResponseFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<TagsOnResponse[]> {
    const args = plainToClass(TagsOnResponseFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,

        response: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TagsOnResponse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async findOne(
    @common.Param() params: TagsOnResponseWhereUniqueInput
  ): Promise<TagsOnResponse | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,

        response: {
          select: {
            id: true,
          },
        },

        tag: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TagsOnResponse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async update(
    @common.Param() params: TagsOnResponseWhereUniqueInput,
    @common.Body() data: TagsOnResponseUpdateInput
  ): Promise<TagsOnResponse | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          response: {
            connect: data.response,
          },

          tag: {
            connect: data.tag,
          },
        },
        select: {
          id: true,

          response: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TagsOnResponse })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async delete(
    @common.Param() params: TagsOnResponseWhereUniqueInput
  ): Promise<TagsOnResponse | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,

          response: {
            select: {
              id: true,
            },
          },

          tag: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
